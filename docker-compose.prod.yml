# Docker Compose override file for production deployment
# Use with: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  wowza-webhook-to-slack:
    # Override image tag for production
    image: wowza-webhook-to-slack:prod
    # Use pre-built image from registry (if you publish to Docker Hub)
    # image: alex-chepurnoy/wowza-webhook-to-slack:latest
    
    # Production logging level
    environment:
      - LOG_LEVEL=WARNING
    
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
    
    # Always restart on failure
    restart: always
    
    # Add labels for monitoring/organization
    labels:
      - "com.example.service=wowza-webhook-to-slack"
      - "com.example.environment=production"
